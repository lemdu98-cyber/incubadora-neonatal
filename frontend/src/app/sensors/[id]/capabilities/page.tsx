import { notFound } from 'next/navigation';import { AppShell } from '@/components/app-shell';import { getMeasurementDefinitions,getSensor,getSensorCapabilities } from '@/lib/api';import { requireSensorCreator } from '@/lib/auth-context';import { CapabilityManager } from './capability-manager';
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;if(!UUID.test(id))notFound();const{user,accessToken}=await requireSensorCreator();const[sensor,definitions,assigned]=await Promise.all([getSensor(id,accessToken),getMeasurementDefinitions(accessToken),getSensorCapabilities(id,accessToken)]);return <AppShell user={user} active="sensors"><h1 className="text-3xl font-bold">Capacidades de {sensor.code}</h1><p className="mt-2 text-slate-600">Selecciona explícitamente las magnitudes que puede producir este sensor.</p><CapabilityManager sensor={sensor} definitions={definitions} initialAssigned={assigned}/></AppShell>}
