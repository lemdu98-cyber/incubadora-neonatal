import{notFound}from'next/navigation';import{AppShell}from'@/components/app-shell';import{getGuardians,getPatient}from'@/lib/api';import{requireClinicalRole}from'@/lib/auth-context';import{AddGuardianForm}from'./add-guardian-form';export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const{user,accessToken}=await requireClinicalRole();const[patient,guardians]=await Promise.all([getPatient(id,accessToken).catch(()=>null),getGuardians(accessToken)]);if(!patient)notFound();return <AppShell user={user} active="patients"><h1 className="text-3xl font-bold">Agregar tutor</h1><p className="mt-2">Paciente: {patient.firstName} {patient.lastName}</p><AddGuardianForm patientId={id} guardians={guardians}/></AppShell>}
