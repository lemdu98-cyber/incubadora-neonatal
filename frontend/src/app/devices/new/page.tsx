import Link from'next/link';import{AppShell}from'@/components/app-shell';import{getIncubators}from'@/lib/api';import{requireDeviceCreator}from'@/lib/auth-context';import{DeviceForm}from'../device-form';const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;export default async function Page({searchParams}:{searchParams:Promise<{incubatorId?:string}>}){const{user,accessToken}=await requireDeviceCreator();const incubators=await getIncubators(accessToken),{incubatorId}=await searchParams,initial=incubatorId&&UUID.test(incubatorId)&&incubators.some(i=>i.id===incubatorId)?incubatorId:undefined;return <AppShell user={user} active="devices"><p><Link href="/devices">Dispositivos</Link> / Nuevo</p><h1 className="mt-5 text-3xl font-bold">Nuevo dispositivo</h1><DeviceForm incubators={incubators} initialIncubatorId={initial}/></AppShell>}
